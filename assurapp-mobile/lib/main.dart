import 'package:flutter/material.dart';

void main() => runApp(const AssurApp());


class AssurApp extends StatelessWidget {
  const AssurApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}